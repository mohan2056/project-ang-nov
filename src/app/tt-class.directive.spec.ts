import { TtClassDirective } from './tt-class.directive';

describe('TtClassDirective', () => {
  it('should create an instance', () => {
    const directive = new TtClassDirective();
    expect(directive).toBeTruthy();
  });
});
